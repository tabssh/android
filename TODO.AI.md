# TODO

- [ ] F-Droid listing screenshots: capture phone screenshots on an
  emulator/device and add them under
  `metadata/en-US/images/phoneScreenshots/` (optional for inclusion,
  strongly recommended for the store listing). Requires a running
  emulator — see `scripts/android-emulator.sh`.
