# TODO.AI.md — v1.0 Production Bug Audit (2026-09-14)

Aggregated confirmed findings from the scoped audit agents. Every item must be
fixed before the single release commit. No feature additions, no regressions.

## ui/adapters, ui/views, ui/utils, ui/keyboard, ui/models (audit-adapters)

- [x] A1 [MAJOR] ui/views/TerminalView.kt:1981 — touch-path row conversion adds scroll offset (`screenRow + scrollRows`) while render path subtracts it (line 1595); same inverted formula in detectUrlAtPosition (line 2153). When scrolled into scrollback, double-tap word select, long-press URL detection, and selectWordAtPosition act on a row 2×scrollRows newer than touched or fail bounds check. Fix: change `+` to `-` in getTextAtPosition and detectUrlAtPosition to match render mapping (getSelectedText at 3190 is already correct).
- [x] A2 [MINOR] ui/views/PerformanceOverlayView.kt:74 — cumulative session byte totals displayed as "KB/s" rate from never-reset AtomicLong counters (PerformanceManager.kt:408-446); figure climbs monotonically. Fix: compute delta since previous sample / interval (or relabel as total).
- [x] A3 [MINOR] ui/adapters/FileAdapter.kt:139 — selected state checked via structural equality on RemoteFileInfo while pruneSelection (79) retains by name key; file whose size/mtime changes renders unselected but getSelectedRemoteFiles() (64) returns stale object. Fix: membership by same name/path key used for pruning; refresh stored objects on list update.
- [x] A4 [MINOR] ui/views/VncView.kt + ui/views/SpiceView.kt — single-finger ACTION_DOWN sends left-button press; ACTION_POINTER_DOWN (pinch start) never sends release — every pinch leaks a click-drag into the guest. Fix: send left-button release at original position when transitioning single-touch → pinch.
- [x] A5 [MINOR] ui/utils/ConnectionLauncher.kt:64-93 — hardcoded English dialog strings ("There is already an active session…", "Reattach", "New connection", "Cancel"). Fix: strings.xml.
- [x] A6 [MINOR] ui/adapters/ConnectionAdapter.kt:119,128-138,152-158 — hardcoded "• $groupName", English-composed contentDescription, auth labels "Password"/"SSH Key"/"2FA". Fix: string resources with placeholders.
- [x] A7 [MINOR] ui/adapters/AuditLogAdapter.kt:38,52 — SimpleDateFormat("MMM dd, HH:mm:ss", Locale.US) + hardcoded "No additional details". Fix: locale-aware DateFormat + string resource.
- [x] A8 [MINOR] ui/adapters/SyncLogAdapter.kt:37 — same Locale.US SimpleDateFormat. Fix: locale-aware formatting.
- [x] A9 [MINOR] ui/adapters/FileAdapter.kt:51 — hardcoded "MMM dd, HH:mm" pattern. Fix: DateFormat.getDateTimeInstance()/DateUtils.
- [x] A10 [MINOR] ui/models/ConnectionListItem.kt:42 — hardcoded "Ungrouped ($connectionCount)". Fix: string resource with placeholder.
- [x] A11 [MINOR] ui/adapters/IdentityAdapter.kt:46 — hardcoded "Username: ${identity.username}". Fix: string resource with placeholder.
- [x] A12 [MINOR] ui/keyboard/MultiRowKeyboardView.kt:420 — hardcoded "← Back" key label. Fix: string resource.
- [x] A13 [MINOR] ui/adapters/GroupedConnectionAdapter.kt:184-185 — group indent via left-only setPadding; wrong side in RTL. Fix: setPaddingRelative (start).
- [x] A14 [MINOR] ui/views/TerminalView.kt:2670 — accessibility contentDescription hardcoded "SSH Terminal". Fix: string resource.
- [x] A15 [MINOR] storage/database/entities/Identity.kt getAuthTypeDisplay() — returns hardcoded English auth labels; adapters should resolve labels via string resources (auth_type_password / identity_auth_type_ssh_key etc.) instead of the entity method. Found during A6/A11 fix.
- [x] A16 [MINOR] ui/models/ConnectionListItem.kt GroupHeader.getDisplayText — "name (count)" composed in locale-free model; move composition to adapter with a format string resource (parallel to A10's UngroupedHeader fix). Found during A10 fix.

## utils, automation, themes, widget, tracker, root package (audit-utils)

- [x] U1 [MAJOR] tracker/VpsMarkdownImportExport.kt:104 — pre-bracket token split uses Regex("\s*-\s*"); \s* matches empty so hyphenated hostnames ("my-host") split into separate tokens, shifting hostname/IPv4/IPv6 columns; export→import round trip silently corrupts every such row. Fix: Regex("\s+-\s+") (or split on literal " - ").
- [x] U2 [MAJOR] themes/definitions/BuiltInThemes.kt:~849-882 — rosePine() has ANSI green (0xFF9CCFD8) and blue (0xFF31748F) swapped vs official palette (green=#31748f pine, blue=#9ccfd8 foam); breaks byte-identical cross-platform guarantee. Fix: swap the two values; re-check cyan=#ebbcba.
- [x] U3 [MAJOR] themes/definitions/ThemeManager.kt:72-103 — installBuiltInThemes() inserts only missing themeIds, so shipped color fixes to built-ins never reach existing installs. Fix: re-upsert built-in rows on version change (REPLACE by themeId, keyed off versionCode or content hash).
- [x] U4 [MAJOR] themes/definitions/BuiltInThemes.kt getAllThemes() — systemDefault() reuses systemDark()/systemLight() themeId so the 23-theme list holds only 22 unique ids; DB stores 22 rows, picker shows duplicate name, "System Default" persists a fixed dark/light id and stops following the system. Fix: dedicated "system_default" themeId resolved dark/light at render time.
- [x] U5 [MINOR] themes/parser/ThemeParser.kt colorToHex — "%06X" strips alpha, so 0x44-alpha selection colors export opaque and re-import changes the look; primaryVariant/onSecondary written on export but ignored on import. Fix: emit #AARRGGBB when alpha != 0xFF; parse both fields on import.
- [x] U6 [MINOR] automation/TaskerWorker.kt:115 + LocaleFireReceiver.kt:44 / TaskerActionReceiver.kt:49 — tasker_command_timeout pref has no effect: receivers always put KEY_TIMEOUT_MS (hardcoded/defaulted to DEFAULT_TIMEOUT_MS) so defaultCommandTimeoutMs() is never consulted. Fix: omit extra when caller didn't supply one; worker falls back to the preference.
- [x] U7 [MINOR] tracker/DomainCsvImportExport.kt:130,147 — parse() is line-based after blank-line filtering, so quoted fields with embedded newlines (which export legitimately produces) split into broken rows; warning line numbers drift. Fix: quote-aware record scanner across whole text; number warnings by original line.
- [x] U8 [MINOR] utils/NotificationHelper.kt:~726 — maybeAlertForHost alertId (10_000 + hash%90_000) xor 0x40000 lands in 272_144-362_143, overlapping host-monitoring (200_000-289_999) and container-update (300_000-389_999) ranges — collisions replace/cancel unrelated notifications. Fix: dedicated non-overlapping base (e.g. 500_000 + hash%90_000).
- [x] U9 [MINOR] utils/PowerLockHelper.kt:54-65 — acquireTimedWakeLock() overwrites wakeLock without releasing a still-held prior lock; interleaving with screen-on transition (SSHConnectionService.kt:902) orphans a held indefinite PARTIAL_WAKE_LOCK — battery drain. Fix: release any held lock at top of acquireTimedWakeLock().
- [x] U10 [MINOR] widget/ConnectionWidgetProvider.kt:141-142 — 2x1 empty state hardcodes "TabSSH" / "Tap to configure" while larger layouts use resources. Fix: R.string.app_name / R.string.widget_tap_to_configure.

## containers + sftp (audit-containers)

- [x] C1 [BLOCKER] containers/transport/DockerCliParsers.kt:183 — parseCliVersion requires a "Server" JSON object, but local Podman `podman version --format '{{json .}}'` sets only Client (verified against podman source) — on socketless Podman hosts the cli_exec tier (the only tier) fails detection for a working install, breaking four-engine parity. Fix: accept Client when Server absent; handle Podman "APIVersion" key casing.
- [x] C2 [MAJOR] containers/transport/DockerCliParsers.kt:93-100,196 — parseContainerLine reads Docker key shapes ("ID", comma-string Names, k=v Labels) but Podman ps JSON uses "Id", Names as array, Labels as map; parseCliInfo and images/stats/df parsers have the same Docker-shape assumption — Podman CLI tier yields empty ids (start/stop/logs target ""), garbled names, lost compose labels. Fix: Podman-aware parsing (accept both key shapes) across CLI-tier parsers.
- [x] C3 [MAJOR] containers/transport/CliExecTransport.kt:168 — pullImage omits cliContext.envPrefix() (unlike streamLogs:126/streamStats:135) — with a tcp:// or non-default socket endpoint, pull runs against the wrong daemon and recreate won't find the image. Fix: prepend cliContext.envPrefix().
- [x] C4 [MAJOR] containers/ContainerSessionManager.kt:158,190-209 — connectForMonitoring succeeds but detector.detect() failure paths return without cleanup — monitoring-only SSH connection never enters the pool, has no notification, stays open until process death. Fix: on any non-Success detect result close the connection (when opened monitoring-only for this acquire and no other pool session shares the profile).
- [x] C5 [MINOR] containers/transport/SshExecRunner.kt:140,152 — stream() decodes each chunk independently; multibyte UTF-8 split across chunk boundary becomes replacement chars — mojibake in streamed logs/pull output. Fix: persistent CharsetDecoder or accumulate bytes, split on newline bytes before decoding.
- [x] C6 [MINOR] sftp/SCPClient.kt:135,247,311,328 — SCP headers encoded US_ASCII, non-ASCII → '?'; "café.jpg" uploads as "caf?.jpg" and such names collide. Fix: encode headers UTF-8.
- [x] C7 [MINOR] sftp/SCPClient.kt:147-155 — uploadFile loop never checks task.isCancelled() and complete(Success) fires after cancel — cancelled single-file upload keeps transferring then flips CANCELLED→COMPLETED. Fix: check isCancelled() in loop; complete(Cancelled) before success path (mirror directory path at 301).
- [x] C8 [MINOR] containers/transport/RemoteExecOps.kt:200,385 — STANDALONE compose branch probes/runs literal docker-compose on every engine while composeMissing() tells Podman users to install podman-compose which is never probed. Fix: probe/run podman-compose for PODMAN.
- [x] C9 [MINOR] containers/transport/IncusCliTransport.kt:299 — pull alias via substringAfterLast(':'/'/') collapses "ubuntu/22.04" and "ubuntu-minimal/22.04" to "22.04" — second image copy --alias fails or repoints alias. Fix: derive alias from full ref with separators mapped to '-'.
- [x] C10 [MINOR] containers/transport/IncusApiTransport.kt:499-522 — pullImage capped at OPERATION_TIMEOUT_MS (300s) then reports failure while daemon-side pull continues and succeeds — retry re-downloads. Fix: longer/progress-reset deadline for image pulls.

## storage (audit-storage)

- [x] S1 [MINOR] storage/database/SystemGroupHelper.kt:26 — getOrCreateSystemGroupId does unsynchronized get-then-insert (Flow lookup then insertGroup, no transaction/lock); concurrent callers sharing a groupType ("vm_hosts" from 4 manager activities, "cloud" from CloudAccountsActivity) can insert duplicate system groups splitting connections. Fix: Mutex in the object, or lookup+insert inside db.withTransaction with a direct query instead of Flow.first().
- [x] S2 [MINOR] storage/preferences/PreferenceManager.kt:749 — Pastebin API key stored plaintext in default SharedPreferences while IDEA.md makes Keystore/encrypted storage the only trusted secret stores. Fix: route through SecurePasswordManager under pastebin_api_key alias, keeping getter/setter signatures so sync (SyncDataCollector.kt:1264) and backup (BackupExporter.kt:791) wire formats stay unchanged.
- [x] S3 [MINOR] storage/database/dao/ConnectionDao.kt:26 — searchConnections dead (also searchKeys KeyDao.kt:72, searchThemes ThemeDao.kt:69); all three take raw LIKE pattern so a future bare-string caller silently matches nothing. Fix: remove the three unused DAO methods (or add '%' || :query || '%' like SnippetDao.searchSnippets if kept).
- [x] S4 [MINOR] storage/database/entities/ConnectionProfile.kt:14 (+HypervisorProfile.kt:122, MonitorSlot.kt:29) — class-doc references nonexistent migrations (MIGRATION_36_37, v29→v30, v30→v31, v17→v18, v23→v24, v24→v25, pre-v38, v28→v29, v32) beyond the actual v27 ceiling. Fix: rewrite comments against the real registered chain (MIGRATION_3_4…MIGRATION_26_27 in TabSSHDatabase.kt).

## build, manifest, resources, CI (audit-build-res)

- [x] B1 [BLOCKER] .github/workflows/release.yml:222 — "Build F-Droid compatible APK" smoke step runs ./gradlew assembleFdroidRelease with no env: block; without -PfdroidBuild=true fdroidRelease keeps signingConfigs.release and the taskGraph guard (app/build.gradle:384-408) throws missing-KEYSTORE_PASSWORD — every v* stable release run fails before assets are staged. Fix: add the same KEYSTORE_PASSWORD/KEY_PASSWORD env block used at release.yml:204.
- [x] B2 [MAJOR] app/src/main/AndroidManifest.xml:646 — WidgetConfigActivity exported="false" while widget_1x1_info.xml:9 and widget_2x1_info.xml:9 name it as android:configure — launchers get SecurityException and the widget can never be added. Fix: exported="true" on this activity; keep APPWIDGET_CONFIGURE intent-filter and validate incoming appWidgetId.
- [x] B3 [MINOR] .github/workflows/ci.yml:41, development.yml:63, beta.yml:58, release.yml:65 — container: jobs omit options: "--user 0:0" mandated by AI.md PART 12. Fix: add under each container block.
- [x] B4 [MINOR] build.gradle:33 + config/dependency-check-suppressions.xml — suppressions in root config/ dir which AI.md PART 12 forbids (prescribes .github/dependency-check-suppressions.xml). Fix: move file to .github/ and update suppressionFile path.
- [x] B5 [MINOR] .github/workflows/ci.yml:184 — test -f "SPEC.md" checks a nonexistent file and the &&-list silently passes — dead validation; project CLAUDE.md also references nonexistent SPEC.md. Fix: drop/point the CI line at AI.md; fix the CLAUDE.md reference.
- [x] B6 [MINOR] docker/docker-compose.yml:66 — tabssh-build service runs assembleDebug without KEYSTORE_PASSWORD passthrough; compose doesn't inherit host env and debug signs with release config → always dies on the taskGraph GradleException. Fix: add KEYSTORE_PASSWORD=${KEYSTORE_PASSWORD:-password} (and KEY_PASSWORD) matching Makefile ?= defaults.
- [ ] B7 [NOTE] AI.md PART 13 stale note about development.yml ephemeral-keystore fallback — doc drift only; AI.md is readonly by convention (SPEC.md would carry overrides). Do NOT edit AI.md without user say-so; flag to user instead.

## ui/fragments, ui/dialogs, ui/tabs (audit-fragments)

- [x] F1 [MAJOR] ui/fragments/SshHostsFragment.kt:75 — multi-select non-functional: selectedConnections only cleared (:422,:515) and read (:443,:447), never populated; ConnectionAdapter has no selection support; enterSelectionMode only swaps flat adapter long-click while grouped view unchanged — bulk edit/delete always "no connections selected". Fix: real selection tracking in adapters (toggle on tap populating selectedConnections).
- [x] F2 [MAJOR] ui/fragments/AuthKeysFragment.kt:51 — installKeyScope cancelled in onDestroyView (:146), never recreated; installKeyOnServer (:311) passes it to SSHConnection.connect() which launches into that dead scope — after first tab switch, "Install on server" always fails with misleading "Authentication failed". Fix: recreate scope in onViewCreated or use per-call/application scope.
- [x] F3 [MAJOR] ui/fragments/TelnetHostsFragment.kt — Telnet hosts have no Connect action anywhere (tap=edit, context menu=Edit/Delete only) though a working connect path exists (SSHTab.kt:664 telnet, TabTerminalActivity.kt:2621, ConnectionLauncher); KDoc "no live Telnet connector yet" is false. Fix: add Connect action routed through ConnectionLauncher like SSH/VNC hosts.
- [x] F4 [MAJOR] ui/fragments/containers/ContainerHostsFragment.kt:274 — multi-step delete cascade (child deletes, tombstone, host delete) in view scope, no Room transaction; leaving screen mid-delete cancels partway (sync resurrects host). Same pattern: HypervisorsFragment.kt:263, ContainerStacksFragment.kt:359, CloudAccountsFragment.kt:746, SshHostsFragment.confirmAndBulkDelete:475. Fix: database.withTransaction on non-view scope (pattern per AuthKeysFragment.deleteSshKey:799).
- [x] F5 [MAJOR] ui/fragments/PerformanceFragment.kt:614 — onDestroyView launches SSH disconnect in the fragment lifecycleScope which is cancelled immediately after — disconnect skipped, SSHConnection stays connected for process lifetime. Fix: launch teardown in app.applicationScope (as SSHTab.disconnectBootstrapSession:785).
- [x] F6 [MINOR] ui/fragments/AuthSshFragment.kt:188 — PASSWORD_MASK decided from DB password column but identities persist password=null with secret in Keystore (identity_${id}) — editing never shows mask, user may wipe/re-enter. Fix: query Keystore asynchronously like AuthVncFragment/AuthVmsFragment.
- [x] F7 [MINOR] ui/fragments/containers/ContainerStacksFragment.kt:357 — deleteStack runs composeDown, DB delete, TombstoneRecorder.record on main dispatcher, no withContext(IO) — jank/ANR. Fix: withContext(Dispatchers.IO) + transaction per F4.
- [x] F8 [MINOR] ui/fragments/AuthVmsFragment.kt:204 — TombstoneRecorder.record on main dispatcher in delete path — main-thread DB I/O. Fix: move delete+record into withContext(Dispatchers.IO).
- [x] F9 [MINOR] ui/fragments/AuthVmsFragment.kt:196 — delete dialog positive button silently no-ops when hypervisors still link the credential. Fix: show identity_vm_cred_linked_hypervisors (strings.xml:3584) message instead of silent return.
- [x] F10 [MINOR] ui/dialogs/RegistryCredentialDialog.kt:219 — Keystore store() Boolean ignored; failure still shows "saved" toast and dismisses with no usable secret. Fix: check return, surface error, don't dismiss.
- [x] F11 [MINOR] ui/fragments/CloudAccountsFragment.kt:566,823 — non-OCI save/edit-token paths ignore storePassword Boolean — Keystore failure leaves orphaned account row + false "saved" toast. Fix: check and roll back insert on failure as saveOrUpdateOciAccount (:517/:529) does.
- [x] F12 [MINOR] ui/fragments/CloudAccountsFragment.kt:711 (+ :546,:574,:615,:757,:832) — catch(Exception) swallows CancellationException → spurious "Refresh failed: cancelled" dialogs. Fix: catch (e: CancellationException) { throw e } before each generic catch.
- [x] F13 [MINOR] ui/fragments/SshHostsFragment.kt:527 — exitSelectionMode re-calls setupRecyclerView() launching an additional permanent connectionStates.collect (:232) each cycle — stacking duplicate collectors. Fix: start flow collection exactly once per view, outside setupRecyclerView.
- [x] F14 [MINOR] ui/fragments/FrequentConnectionsFragment.kt:60,159 — connectionStates collected in lifecycleScope without viewLifecycleOwner/repeatOnLifecycle (collector touches views while stopped); catch swallows CancellationException. Fix: viewLifecycleOwner.lifecycleScope + repeatOnLifecycle(STARTED), rethrow.
- [x] F15 [MINOR] ui/dialogs/ReportIssueDialog.kt:273 — submit catch swallows CancellationException and reports failures with SFTP string sftp_upload_failed_fmt — spurious mislabeled "Upload failed" on dismiss. Fix: rethrow CancellationException; report-specific error string.
- [x] F16 [MINOR] ui/fragments/PerformanceFragment.kt:244,515-518,334,384,404,448 — hardcoded English UI strings + missing CancellationException rethrow at :448. Fix: strings.xml + rethrow guard.

## ssh, terminal, protocols, network, services, background (audit-ssh-core)

- [x] K1 [MAJOR] ssh/forwarding/PortForwardCoordinator.kt:172 — stop(pfId) closes the shared pooled SSH session via closeConnection(handle.endpointKey) without checking other consumers — stopping a port forward disconnects a live terminal tab on the same profile. Fix: track whether the coordinator created the connection; only close coordinator-owned sessions.
- [x] K2 [MINOR] ssh/connection/SSHConnection.kt (setupJumpHost) — setHostKeyAlias(profile.host) unconditional; JSch appends "[host]:port" only when alias null and port != 22 — non-22 target via jump host TOFU-stored under bare hostname, divergent known-hosts rows, duplicate prompts. Fix: alias "[${profile.host}]:${profile.port}" when port != 22.
- [x] K3 [MINOR] ssh/connection/SSHConnection.kt (connectWithSilentRetry) — retry passes profile.host instead of resolveHostForIpMode() literal — force-IPv4/IPv6 lost on silent retry. Fix: reuse resolved host literal.
- [x] K4 [MINOR] ssh/connection/SSHConnection.kt (handleConnectionError) — logAuthFailure called for every connection error including timeouts — audit log fills with fake auth failures. Fix: log auth failure only for JSch authentication rejections.
- [x] K5 [MINOR] ssh/config/SSHConfigParser.kt:161 — multi-pattern "Host a b c" imported as one profile with host "a b c". Fix: split Host value on whitespace, one profile per concrete pattern (or skip multi-pattern lines).
- [x] K6 [MINOR] terminal/emulator/ANSIParser.kt:194,209 — legacy emulator: IND uses buffer bottom not scrollBottom, RI checks row<0 not scrollTop, NEL never scrolls at bottom; colon-form SGR (38:5:n/38:2:r:g:b) dropped; ECH/ICH/VPA unimplemented — display corruption with DECSTBM regions in classic non-swipe mode. Fix: clamp IND/RI/NEL to active scroll region; accept colon-separated SGR.
- [x] K7 [MINOR] network/portknock/PortKnocker.kt:88 — knockTCP blocking connect(...,2000) stalls 2s per DROPped port; 3-4 port sequence (4-8s) overruns typical knockd seq_timeout — knock silently fails. Fix: fire SYN without waiting (~200ms timeout / non-blocking connect closed immediately).
- [x] K8 [MINOR] ssh/forwarding/PortForwardingManager.kt (tunnelStates) — public StateFlow only updated on create/remove, never on ACTIVE/STOPPED/ERROR, zero consumers — dead/stale API. Fix: emit on every transition or remove the flow.

## hypervisor + cloud (audit-hypervisor)

- [x] H1 [BLOCKER] hypervisor/xcpng/XCPngApiClient.kt:142 — authenticate() substrings the FIRST <value> of the XML-RPC response, but XAPI wraps responses in <value><struct> with Status/Value members — sessionId becomes struct garbage; every direct XCP-ng call fails SESSION_INVALID. Same first-<value> flaw in parseStringResponse (358), parseIntResponse/parseBooleanResponse, parseArrayResponse (384) — corrupts names/power states, getVMRefByUUID (522) always null, drops first array element; power actions return true on XAPI Failure since Status unchecked. Fix: one shared parser extracting the Value member and failing on Status=Failure; encode boolean params as <boolean>0|1</boolean> (startVM:216).
- [x] H2 [BLOCKER] hypervisor/xcpng/XenOrchestraApiClient.kt:395 — listVMs does JSONArray(body).getJSONObject(i) on GET /rest/v0/vms without ?fields=, but XO returns href strings by default → JSONException swallowed at 438 → always emptyList; XO mode shows zero VMs (same in listPools/listHosts/listSnapshots/backups). Fix: request ?fields=id,uuid,name_label,power_state,memory,VCPUs_max,type,tags,mainIpAddress,$pool,$container,os_version (or dereference hrefs).
- [x] H3 [MAJOR] hypervisor/xcpng/XenOrchestraApiClient.kt:500,532,562,593,623,653 — power actions POST /rest/v0/vms/{id}/start etc., but XO exposes actions only at POST /rest/v0/<type>/<uuid>/actions/<action> (actions/start, clean_shutdown, hard_shutdown, clean_reboot, hard_reboot) — every XO power op 404s. Fix: use /actions/ paths with XO action names.
- [x] H4 [MAJOR] cloud/HetznerClient.kt:114,117 — start/stop send power_on/power_off but Hetzner endpoints are /actions/poweron and /poweroff — always 404. Fix: rename to poweron/poweroff.
- [x] H5 [MAJOR] cloud/AzureVmClient.kt:213,237 — forceRestartInstance passes "restart?skipShutdown=true" as action; URL template appends ?api-version → 400 MissingApiVersionParameter. Fix: action "restart", append &skipShutdown=true after api-version.
- [x] H6 [MAJOR] cloud/AwsEc2Client.kt:227,266 — non-greedy Regex("<item>([\s\S]*?)</item>") truncates at nested </item> (groupSet/tagSet/blockDeviceMapping/networkInterfaceSet) — Name tags lost, leftover fragments create phantom instances with empty ids. Fix: XmlPullParser tracking item depth (or nesting-balanced matching requiring <instanceId>).
- [x] H7 [MAJOR] hypervisor/proxmox/ProxmoxApiClient.kt:367 + ProxmoxManagerActivity.kt:662-669 — resetVM posts status/reset for LXC too, but PVE LXC has no reset endpoint; Reset button shown for every running guest incl. lxc — always errors. Fix: hide Reset for lxc (or route to status/stop + status/start).
- [x] H8 [MAJOR] hypervisor/xcpng/XenOrchestraApiClient.kt:1356 — connectWebSocket sends invented {"type":"authenticate"} to wss://host/api/ (a JSON-RPC 2.0 endpoint; auth is session.signInWithToken) and matches invented event types ("vm.started") — event stream never authenticates/delivers. Fix: speak JSON-RPC 2.0 (session.signInWithToken + `all` notifications) or drop socket and poll.
- [x] H9 [MAJOR] hypervisor/xcpng/XenOrchestraApiClient.kt:1553,1561 — fallback console URL /api/console/{vmId} but xo-server registers ^/api/consoles/(.*)$ (plural) — fallback 404s, console never connects. Fix: /api/consoles/{vmId}.
- [x] H10 [MINOR] cloud/GcpComputeClient.kt:146-150 — 401/403 throws IllegalStateException instead of CloudAuthException (all other providers) — expired GCP credential misses re-add flow. Fix: add 401/403 → CloudAuthException branch.
- [x] H11 [MINOR] cloud/GcpComputeClient.kt:176,222 — instance id is bare GCE name; zone resolved by first name match — same-name instances in multiple zones can target wrong zone. Fix: use zone/name (or numeric id) as CloudInstanceState id.
- [x] H12 [MINOR] cloud/GcpComputeClient.kt:166-171 — SUSPENDED/SUSPENDING unmapped → "unknown". Fix: map SUSPENDED→stopped(/suspended), SUSPENDING→stopping.
- [x] H13 [MINOR] cloud/AzureVmClient.kt:188,224 — instance id is bare VM name; resource group by first name match — same-name VMs in different groups collide. Fix: key by resourceGroup/name.
- [x] H14 [MINOR] cloud/OciCloudClient.kt:151 — TERMINATED mapped to "stopped" — deleted instances appear stopped with a Start that 409s. Fix: skip TERMINATED/TERMINATING (or distinct terminated status).

## sync, backup, crypto, pairing, audit (audit-sync-crypto)

- [x] Y1 [MAJOR] sync/encryption/SyncEncryptor.kt:149,405-416 — code writes magic "TABSSH_SYNC_V3" and hard-rejects everything else while IDEA.md freezes "TABSSH_SYNC_V2"; no V2 fallback reader — V2 sync files fail until re-seeded. Fix: investigate commit 3665e49c (deliberate V3 bump?); add read-only V2 fallback OR reconcile IDEA.md — decide during implementation, check with user if genuinely ambiguous.
- [x] Y2 [MAJOR] sync/merge/SyncMergeCoordinator.kt:181-209 — auto-resolve silently discards conflicts autoResolveConflicts can't resolve (host-key mismatch/MITM, DELETED_MODIFIED, equal-timestamp ties → null at ConflictResolver.kt:270): never applied/persisted/logged yet base snapshot advances. Fix: route non-auto-resolvable and tied conflicts into pendingSyncConflictDao + Sync Log.
- [x] Y3 [MAJOR] backup/import/BackupImporter.kt:378-384 — restoreKeys uses plain @Insert (ABORT) with no per-item catch inside all-or-nothing transaction — merge restore over an existing key rolls back the ENTIRE restore. Fix: upsert/REPLACE path or per-item catch.
- [x] Y4 [MAJOR] backup/import/BackupImporter.kt:357-363 — overwrite-mode REPLACE on connections with PRAGMA foreign_keys=ON fires ON DELETE CASCADE — restore silently deletes local tab_sessions and audit_log children. Fix: update-in-place for existing ids.
- [x] Y5 [MAJOR] sync/worker/SyncWorkScheduler.kt:26-58,145 — parseSyncFrequency("manual")=0 but schedulePeriodicSync always enqueues periodic work (clamped to 15 min) — "Manual only" users get background sync every 15 min. Fix: frequency 0 → cancel periodic and return.
- [x] Y6 [MAJOR] crypto/SSHKeyGenerator.kt:251-254 — passphrase-protected Ed25519 export via JcePEMEncryptorBuilder+MiscPEMGenerator emits nonstandard "BEGIN PRIVATE KEY" with DEK-Info-encrypted PKCS#8 body — unreadable by OpenSSH, OpenSSL, and the app's own parser: unrecoverable data loss; kdoc claim false. Fix: export encrypted Ed25519 in OpenSSH v1 format (bcrypt KDF, already implemented for import) or JceOpenSSLPKCS8EncryptorBuilder.
- [x] Y7 [MAJOR] crypto/storage/SecurePasswordManager.kt:301-327,353-379 — biometric store/retrieve resume suspendCancellableCoroutine from onAuthenticationFailed (fires per bad attempt with prompt open) — later success/error double-resumes → IllegalStateException crash. Fix: resume only from onAuthenticationSucceeded/onAuthenticationError or atomic resumed flag.
- [x] Y8 [MINOR] backup/export/BackupExporter.kt:791 — pastebinApiKey written into plaintext preferences.json (reimported at BackupImporter.kt:935), leaking secret in unencrypted backups. Fix: move into encrypted secrets.json payload. (Pairs with S2.)
- [x] Y9 [MINOR] backup/import/BackupImporter.kt:259-341 — replace-mode restore of legacy v3 archive clears connections; CASCADE wipes local audit_log/tab_sessions that v3 archives never contained. Fix: preserve (or explicitly warn about) audit rows when archive has no audit_log entry.
- [x] Y10 [MINOR] backup/import/BackupImporter.kt:500-505 — restoreHypervisorAccounts plain @Insert with no update fallback — overwrite mode doesn't overwrite existing rows. Fix: update when insert fails for existing id.
- [x] Y11 [MINOR] sync/SAFSyncManager.kt:278-281 — sync file rewritten in place, no temp-then-rename — process death mid-write leaves truncated ciphertext all peers fail to decrypt. Fix: sibling temp document + rename (or detect-and-rewrite on next sync).
- [x] Y12 [MINOR] sync/observer/DatabaseChangeObserver.kt:62-83 — sync-on-change watches only 6 of ~27 synced tables — unwatched entity edits wait for periodic run. Fix: extend tracked tables to all synced entities.
- [x] Y13 [MINOR] pairing/PairingImporter.kt:63,83 — duplicate-name filters computed once pre-import — two same-named entries in one QR both insert. Fix: also track names inserted during the run.
- [x] Y14 [MINOR] pairing/PairingImporter.kt — multi-entity import has no wrapping transaction — mid-import failure leaves half-imported pairing. Fix: database.withTransaction.
- [x] Y15 [MINOR] sync/encryption/SyncEncryptor.kt:284-287 (+pairing/PairingDecryptor.kt) — derived AES key bytes never zeroed. Fix: wipe in finally after building SecretKeySpec.
- [x] Y16 [MINOR] crypto/SSHKeyGenerator.kt:464-468 (+SSHKeyParser.kt:855-859, KeyStorage.kt:842-854) — fingerprints hash X.509/SPKI DER instead of OpenSSH wire blob — "SHA256:..." never matches ssh-keygen/GitHub. Fix: hash the base64-decoded OpenSSH public-key blob.
- [x] Y17 [MINOR] crypto/keys/KeyStorage.kt:1166 — formatPublicKey derives outer type from KeyType.getOpenSSHIdentifier() (switches on constant defaultKeySize) — ECDSA-384/521 exports as ecdsa-sha2-nistp256 with 384/521 blob; sshd rejects. Fix: derive identifier from actual curve as encodeECDSAPublicKey does.
- [x] Y18 [MINOR] audit/AuditLogManager.kt:438-448 — checkAndCleanup only invoked from logCommand — with command logging disabled, retention purge never runs. Fix: also run cleanup from insert (throttled) or periodic schedule.
- [x] Y19 [MINOR] crypto/storage/SecurePasswordManager.kt:68,398 — sessionPasswords plain HashMap accessed from main thread + IO coroutines (comment falsely claims ConcurrentHashMap). Fix: ConcurrentHashMap.
- [x] Y20 [MINOR] crypto/tls/HypervisorCertPromptDialog.kt:185-189 — 30s timeout returns REJECT but dialog stays interactive; later "Accept & pin" mutates consumed result — user thinks cert pinned, nothing pinned. Fix: dismiss dialog from timeout path.

## ui/activities (audit-activities)

- [x] V1 [MAJOR] ui/activities/MultiHostDashboardActivity.kt:956 (also 879-893, 414-429) — stopPump/onDestroy unconditionally disconnect() every ownedSessions entry, but connectForMonitoring reuses live pooled connections incl. ones backing open terminal tabs — leaving dashboard kills active terminal sessions. Fix: track reuse from connectForMonitoring; only disconnect dashboard-dialed sessions.
- [x] V2 [MAJOR] ui/activities/HostDetailActivity.kt:270 — startPump dials via connectForMonitoring but onDestroy only cancels pumpScope, never disconnects a pump-dialed connection — authenticated SSH socket leaks until process death. Fix: disconnect pump-dialed connections in onDestroy when not shared with a terminal tab.
- [x] V3 [MAJOR] ui/activities/RemoteFileEditorActivity.kt:172-188 — rotation-restore branch returns before SFTP setup; sftp stays null and saveFile() (:305) silently no-ops — edits lost after rotation. Fix: re-establish SFTP session in restore path.
- [x] V4 [MAJOR] ui/activities/ImportExportActivity.kt:190-203,257-264,912-924 — restore/export catches swallow CancellationException and show dialogs on destroyed activity → BadTokenException. Fix: rethrow CancellationException; guard with isFinishing/isDestroyed.
- [x] V5 [MAJOR] ui/activities/SFTPActivity.kt — nine catches (:292,:771,:821,:1025,:1153,:1223,:1567,:1668,:1766,:1797) catch bare Exception → dialog on destroyed activity → BadTokenException; only :852 rethrows. Fix: rethrow CancellationException first in each.
- [x] V6 [MAJOR] ui/activities/ConnectionEditActivity.kt — same pattern at :1020,:1275,:1435,:1509,:1543,:1592,:2017,:2286,:2336. Fix: rethrow CancellationException before generic handler.
- [x] V7 [MAJOR] ui/activities/MainActivity.kt:518-525 — transient-VNC quick-connect catch swallows CancellationException, dialog on destroyed activity. Fix: rethrow first.
- [x] V8 [MINOR] ui/activities/PinLockActivity.kt:130 — fail count loaded but no lockout/backoff ever enforced — unlimited full-speed PIN attempts (IDEA.md requires failed-attempt lockout). Fix: escalating delay/lockout after N failures.
- [x] V9 [MINOR] ui/activities/TabTerminalActivity.kt:1649 — openRemoteFileExternally opens ad-hoc SFTPManager channel never closed (RemoteFileOpener keeps it for upload-back, never disconnects) — ~9 opens hit sshd MaxSessions, breaking further channels. Fix: disconnect SFTPManager after open/upload-back round trip.
- [x] V10 [MINOR] ui/activities/TabTerminalActivity.kt:4603,5628,5655 — macro/snippet catches swallow CancellationException → dialog on destroyed activity (narrow window). Fix: rethrow first.
- [x] V11 [MINOR] ui/activities/ImportFromQrActivity.kt — rotation mid-scan loses in-progress import state — screen stuck, no progress/error. Fix: retain operation across config change or finish with message.
- [x] V12 [MINOR] ui/activities/PortForwardingActivity.kt:142-161 — item click captures list index into live-updating currentRoutes — refresh reorder between render and tap acts on wrong route. Fix: resolve clicked item by stable id.
- [x] V13 [MINOR] ui/activities/ThemeEditorActivity.kt:111-117,225-229 — hex EditTexts not refreshed on picker change; Apply reads stale hex overwriting the picked color. Fix: sync hex fields on picker change or read authoritative color state.
- [x] V14 [MINOR] ui/activities/LogViewerActivity.kt:235-267 — level/tag filters only match in-memory ring-buffer format, dropping all file-based lines — filters after process restart show empty/partial list. Fix: parse file lines into the same entry model before filtering.
- [x] V15 [MINOR] ui/activities/SnippetManagerActivity.kt:426-429 — favorite star handler never persists or re-renders. Fix: wire to DAO update + refresh row.
- [x] V16 [MINOR] ui/activities/ComposeEditorActivity.kt:126 — cancel/back discards edited text without unsaved-changes confirmation. Fix: standard discard-confirmation guard.
- [x] V17 [MINOR] ui/activities/ClusterCommandActivity.kt:142,162 — command history persisted as unordered StringSet — recency lost. Fix: JSON array or delimited string.
- [x] V18 [MINOR] ui/activities/CloudAccountManagerActivity.kt:166-172,516-521 — refresh catch swallows CancellationException (spurious failure toast); clear-cache toasts success before delete completes. Fix: rethrow cancellation; toast after result.
- [x] V19 [MINOR] ui/activities/MainActivity.kt:61-66 — onCreate cancels all live per-tab connection notifications even for alive sessions — shade Disconnect actions vanish ~90s until service reposts. Fix: only cancel notifications for tabs that no longer exist.
- [x] V20 [MINOR] ui/activities/ContainerHostEditActivity.kt:746 — probeTransport dials via connectForMonitoring and never disconnects a dialed connection — probe leaks live SSH socket. Fix: disconnect when not a reused session.
- [x] V21 [MINOR] ui/activities/ImportExportActivity.kt:325,354,413,525,749 — secondary catches swallow CancellationException → spurious failure toasts. Fix: rethrow first.
- [x] V22 [MINOR] ui/activities/ConnectionHistoryActivity.kt:87 — load catch swallows CancellationException → spurious toast. Fix: rethrow first.
- [x] V23 [MINOR] ui/activities/SyncLogActivity.kt:146-149 — clear-log deleteAll with no try/catch — Room failure crashes app. Fix: wrap and surface error.
- [x] V24 [MINOR] ui/activities/SettingsActivity.kt:856-863 — audit-log CSV export quotes fields without escaping embedded quotes/newlines — corrupt rows. Fix: RFC 4180 escaping (`"` → `""`).
- [x] V25 [MINOR] ui/activities/LinkHandlerActivity.kt:136-148 — the `content`/`file` scheme branch runs before the `intent.type == MIME_VIRT_VIEWER` check and picks jnlp-vs-virt-viewer from the resolved filename extension only, ignoring `intent.type`. Pre-existing (predates the ACTION_SEND filters); real `.vv`/`.jnlp` files still route correctly, but a share whose source app stripped or renamed the extension misroutes. Fix: consult `intent.type` first, fall back to extension sniffing.
- [x] V26 [MAJOR] sync/models/SyncModels.kt, sync/data/SyncDataCollector.kt, sync/data/SyncDataApplier.kt — Domain and VPS tracker data is never synced: `SyncPayload` has no `domains`/`vpsHosts` field and neither the collector nor the applier touches the tracker DAOs, so trackers are local-only while every other entity type round-trips. Deletes DO record tombstones already (DomainEditActivity.kt:248, DomainTrackerActivity.kt:473, VpsHostEditActivity.kt:266, VpsTrackerActivity.kt:502), but they are orphaned: `enabledTombstoneTypes()`, the live-keys `when` and `snapshotState()` (SyncDataCollector.kt:636-819) never reference DOMAIN/VPS_HOST, so they never travel. Fix: add both collections to `SyncDataPackage`, collect/apply them alongside the other entities, and wire the two tombstone types into the collector. Backup/restore already covers both entities (BackupExporter.kt:214-254) — sync only.
- [x] V27 [MINOR] ui/activities/TabTerminalActivity.kt:5522-5531 — applyHardwareKeyboardPolicy() hides the entire custom key bar whenever `Configuration.keyboard != KEYBOARD_NOKEYS && hardKeyboardHidden == HARDKEYBOARDHIDDEN_NO`, with no user override. Confirmed on the emulator (logs `HW keyboard policy: hw=true pref=true`; bar absent from the uiautomator hierarchy) even with `hw.keyboard = no` in config.ini, because the AVD always forwards the host keyboard. On a tablet with a cover/Bluetooth keyboard (the reporter's Samsung SM-X230) the same policy fires and the bar vanishes — the user sees "custom keyboard not working" while the underlying key path is fine. RFB modifier delivery itself verified correct against a live x11vnc server: Ctrl+A arrives as Control_L, then 'a' with state 0x4 (ControlMask). Fix: keep the stored preference authoritative — let an explicit show override the HW-keyboard auto-hide, or surface a setting, rather than silently forcing GONE.
- [x] V28 [MAJOR] ui/views/VncView.kt, ui/views/SpiceView.kt, ui/activities/TabTerminalActivity.kt — the custom key bar's modifier latch (`consolePendingModifier`) was read in exactly one place, `sendConsoleKeyPress`, which only handles the bar's own key presses. The bar carries no letter keys, so a CTRL/ALT/SFT chord on an RFB/SPICE tab is always completed on the hardware keyboard or the IME — and those reach `VncView`/`SpiceView` directly through `onKeyDown` and the InputConnection's `commitText`, neither of which consulted the latch, so the modifier was silently dropped. Measured against a live x11vnc server: tap CTL on the bar, then 'a' → xev reports `state 0x0` (no ControlMask). Terminal tabs were never affected — `TerminalView.setPendingModifier` has always covered both paths. Fix: mirror that contract in both graphical views (pending modifier, held-modifier bracket released on key-up, `onModifierConsumed`), armed from the same bar latch via `armConsoleViewModifier`, with lock-aware re-arm.
- [x] V30 [MAJOR] ui/activities/TabTerminalActivity.kt, terminal/gestures/PrefixParser.kt — the PRE key did nothing at all on an RFB/SPICE tab while its visual still updated: "PREFIX" sits in `CONSOLE_BAR_ACTION_KEY_IDS` so it never routed to the console, every send in its handler is guarded by `getActiveTerminalView()` (null on a graphical tab), and the fallback `showMultiplexerPickerDialog()` early-returns on `tabManager.getActiveTab()`, which only resolves `Tab.Ssh`. Nothing could reach the guest. Fix: route PREFIX to `handleConsolePrefixKey()` when `activeGraphicalDisplayMode() != null`. Detection cannot probe a framebuffer, so the first tap opens a console-only picker (tmux/zellij/screen — no "auto"), remembers the type per `Tab.tabId`, and sends immediately; later taps send directly and long-press re-opens the picker. The prefix travels as a real modifier+key chord (new `PrefixParser.parseChord`) because RFB carries keysyms and SPICE scancodes, neither of which can carry `parse()`'s control byte. Verified against a live x11vnc server: tmux → xev logs `Control_L` down, `space` with `state 0x4`, both releases; after switching to screen → `Control_L` + `x` with `state 0x4`.
- [x] V29 [MINOR] Makefile:88 — `make test` runs `check` plus `scripts/ui-test.sh`, but never invokes `connectedAndroidTest`, so the instrumented suite is not reachable through any documented target even though AI.md PART 11 calls instrumented tests required before tagging a release. Fix: add a target that runs `connectedAndroidTest` against a reachable emulator/device, and reference it from the release checklist.
- [x] V31 [MAJOR] sync/observer/DatabaseChangeObserver.kt:26-52 — `SYNCED_TABLES` omits `domains` and `vps_hosts`, the two tables added to the sync surface in 5bfaa62. Its own comment says the list must cover every table `SyncDataCollector` reads, so adding or editing a Domain/VPS tracker entry never invalidates an observed table, never debounces, and never enqueues `SyncWorker` — tracker changes only travel on the 60-minute periodic sync or a manual Sync Now, which reads as "trackers do not sync". Collector, applier, tombstones, toggles (default on) and the Sync Settings rows are all correct. Fix: add both table names to `SYNCED_TABLES`.
- [ ] V32 [MINOR] ui/activities/CloudAccountsActivity.kt:90-91, res/layout/activity_cloud_accounts.xml:84, AndroidManifest.xml:491 — dead screen: a standalone Cloud Accounts activity (with its own `fab_add`) that duplicates the Auth > Cloud sub-tab and is reachable from no navigation path. Fix: delete the activity, its layout, and its manifest entry, or route something to it if it is meant to stay.
- [ ] V33 [MINOR] res/layout/activity_vnc_hosts.xml — orphaned layout left behind when `VncHostsActivity` became the Hosts > VNC sub-tab; nothing inflates it and its `fab_add` at :94 is dead. Fix: delete the layout.
- [ ] V34 [MINOR] res/menu/main_menu.xml — menu resource is never inflated anywhere, so every item in it is dead, including `action_create_group` at :25. Fix: delete the menu resource.
- [ ] V35 [MINOR] res/menu/menu_dashboard.xml:5-8 + ui/activities/MultiHostDashboardActivity.kt:410 — the overflow "Add hosts" item calls `showHostPicker(UNGROUPED_ID)`, exactly what the Ungrouped group header's `btn_add_hosts` does. Not a visible double button (overflow is collapsed) and it is the only add path when there are no ungrouped hosts, so it was left in place during the duplicate-affordance sweep. Fix (if wanted): hide the overflow item while the Ungrouped header is on screen.
- [ ] V36 [MINOR] res/menu/terminal_menu.xml:5-9 + res/layout/bottom_sheet_terminal_menu.xml:25 — "New tab" exists in both the toolbar overflow (TabTerminalActivity.kt:3933) and the bottom-sheet menu (:1071). The two are never on screen at the same time, so this is duplication of definition rather than of affordance. Fix (if wanted): drive both from one handler and drop the duplicate entry.
