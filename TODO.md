[x] in new host menu remove docker host if it is linked to ssh host as its just a duplicate
[x] in panes if sync is on highlight all windows instead of the active since sync makes all active
[x] ensure the IDEA.md, README.md, and whats_new.md are all current and matches code
