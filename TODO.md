[ ] in new host menu remove docker host if it is linked to ssh host as its just a duplicate
[ ] in panes if sync is on highlight all w8ndows instead of the active since sync makes all active
[ ] ensure the IDEA.md, README.md, and whats_new.md are all current and matches code 
