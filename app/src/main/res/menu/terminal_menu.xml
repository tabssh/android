<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_new_tab"
        android:title="@string/new_tab"
        android:icon="@drawable/ic_add"
        app:showAsAction="ifRoom" />

    <!-- always: this is the only on-screen way to end a VNC/SPICE session
         (graphical tabs have no in-terminal `exit`), so it must never
         collapse into the overflow menu on narrow toolbars. -->
    <item
        android:id="@+id/action_close_tab"
        android:title="@string/close_tab"
        android:icon="@drawable/ic_close"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_settings"
        android:title="@string/settings_title"
        android:icon="@drawable/ic_settings_general"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_disconnect_all"
        android:title="@string/terminal_menu_disconnect_all"
        android:icon="@drawable/ic_disconnect"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_copy"
        android:title="@string/copy"
        android:icon="@drawable/ic_copy"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_paste"
        android:title="@string/paste"
        android:icon="@drawable/ic_paste"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_select_all"
        android:title="@string/select_all"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_share_session"
        android:title="@string/terminal_menu_share_session"
        android:icon="@drawable/ic_share"
        app:showAsAction="never" />
    
    <item
        android:id="@+id/action_port_forwarding"
        android:title="@string/port_forwarding_title"
        android:icon="@drawable/ic_interface"
        app:showAsAction="never" />
    
    <item
        android:id="@+id/action_toggle_recording"
        android:title="@string/terminal_menu_start_recording"
        app:showAsAction="never" />
    
    <item
        android:id="@+id/action_view_transcripts"
        android:title="@string/terminal_menu_view_transcripts"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_command_palette"
        android:title="@string/terminal_menu_command_palette"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_quick_switcher"
        android:title="@string/terminal_menu_quick_switcher"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_broadcast_input"
        android:title="@string/terminal_menu_broadcast_choose_targets"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_save_workspace"
        android:title="@string/terminal_menu_save_workspace"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_open_workspace"
        android:title="@string/terminal_menu_open_workspace"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_history_palette"
        android:title="@string/terminal_menu_search_history"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_split_bottom"
        android:title="@string/terminal_menu_split_horizontal"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_unsplit"
        android:title="@string/terminal_menu_close_split_pane"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_mosh_handoff"
        android:title="@string/terminal_menu_mosh_handoff"
        app:showAsAction="never" />

    <!-- Issue #173 — recordable macros. Two entries so the user can
         start/stop a recording and pick a saved macro to replay. -->
    <item
        android:id="@+id/action_macro_record"
        android:title="@string/terminal_menu_record_macro"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_macro_replay"
        android:title="@string/terminal_menu_replay_macro"
        app:showAsAction="never" />

</menu>
