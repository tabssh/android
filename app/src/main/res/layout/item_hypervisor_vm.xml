<?xml version="1.0" encoding="utf-8"?>
<!-- Shared VM / instance card used by all hypervisor manager screens. -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/space_sm"
    android:layout_marginBottom="@dimen/space_sm"
    app:cardElevation="@dimen/elevation_low"
    app:cardCornerRadius="@dimen/card_corner_radius_small">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/space_12dp">

        <!-- Header: status dot + name + state badge -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <View
                android:id="@+id/view_status_dot"
                android:layout_width="@dimen/connection_status_indicator_size"
                android:layout_height="@dimen/connection_status_indicator_size"
                android:layout_marginEnd="@dimen/space_sm"
                android:background="@drawable/bg_status_dot" />

            <TextView
                android:id="@+id/vm_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="@dimen/text_size_15sp"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurface" />

            <TextView
                android:id="@+id/vm_state"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_caption"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- CPU / RAM / metadata info line -->
        <TextView
            android:id="@+id/vm_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_xs"
            android:textSize="@dimen/text_size_caption"
            android:textColor="?attr/colorOnSurfaceVariant" />

        <!-- IP address line, shown only when available -->
        <TextView
            android:id="@+id/vm_ip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_2dp"
            android:textSize="@dimen/text_size_caption"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:visibility="gone" />

        <!-- Connect row: VNC console + SSH — shown for running VMs with console/ssh support -->
        <LinearLayout
            android:id="@+id/row_connect"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="@dimen/space_10dp"
            android:visibility="gone">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_console"
                style="@style/Widget.Material3.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="@dimen/space_xs"
                android:minHeight="@dimen/min_touch_target"
                android:minWidth="0dp"
                android:text="@string/hypervisor_vm_action_vnc" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_ssh"
                style="@style/Widget.Material3.Button"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="@dimen/space_xs"
                android:minHeight="@dimen/min_touch_target"
                android:minWidth="0dp"
                android:text="@string/hypervisor_vm_action_ssh" />

        </LinearLayout>

        <!-- Main power row: Start / Stop -->
        <LinearLayout
            android:id="@+id/row_main"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="@dimen/space_sm"
            android:visibility="gone">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_start"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="@dimen/space_xs"
                android:minHeight="@dimen/min_touch_target"
                android:minWidth="0dp"
                android:text="@string/container_action_start" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_stop"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="@dimen/space_xs"
                android:minHeight="@dimen/min_touch_target"
                android:minWidth="0dp"
                android:text="@string/container_action_stop" />

        </LinearLayout>

        <!-- Secondary actions row: Reboot / Reset -->
        <LinearLayout
            android:id="@+id/row_secondary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="@dimen/space_sm"
            android:visibility="gone">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_reboot"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="@dimen/space_xs"
                android:minHeight="@dimen/min_touch_target"
                android:minWidth="0dp"
                android:text="@string/hypervisor_vm_action_reboot" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_reset"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="@dimen/space_xs"
                android:minHeight="@dimen/min_touch_target"
                android:minWidth="0dp"
                android:text="@string/libvirt_reset_button" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
