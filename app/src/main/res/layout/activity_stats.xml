<?xml version="1.0" encoding="utf-8"?>
<!--
  Host screen for the Stats tab, relocated from the main tab strip into the
  nav drawer's Insights group. Wraps the unmodified PerformanceFragment in a
  single-fragment activity so its existing UI/UX is preserved exactly.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/app_bar"
        layout="@layout/include_app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/stats_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
