<?xml version="1.0" encoding="utf-8"?>
<!--
  Home screen. The navigation drawer is supplied by TabSSHActivity's shared
  scaffold, so this layout only declares the home screen's own content.

  The app bar is deliberately a single row — hamburger inline with the tab
  strip — instead of the shared include_app_bar.xml title bar, because a
  separate title row would cost a full bar of vertical space above the tabs.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activities.MainActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primary_500">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@color/primary_500"
            android:gravity="center_vertical">

            <ImageButton
                android:id="@+id/btn_nav_menu"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_menu"
                app:tint="@color/white"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/navigation_drawer_open"
                android:padding="@dimen/space_12dp" />

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tab_layout"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@color/primary_500"
                app:tabTextColor="@color/white"
                app:tabSelectedTextColor="@color/white"
                app:tabIndicatorColor="@color/accent_500"
                app:tabIndicatorHeight="3dp"
                app:tabMode="scrollable"
                app:tabGravity="start" />

        </LinearLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/space_md"
        android:contentDescription="@string/add_connection"
        app:srcCompat="@drawable/ic_add"
        style="@style/Widget.TabSSH.FloatingActionButton" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
