<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_container_host"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_sm"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/elevation_low">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/space_md">

        <!-- Engine icon: decorative only — the engine name is spelled out in
             the badge below, so the glyph is never the sole differentiator. -->
        <TextView
            android:id="@+id/text_engine_icon"
            android:layout_width="@dimen/icon_size_xlarge"
            android:layout_height="@dimen/icon_size_xlarge"
            android:gravity="center"
            android:textSize="@dimen/terminal_text_size_max"
            android:importantForAccessibility="no" />

        <!-- Info Column -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/space_md"
            android:orientation="vertical">

            <!-- Name -->
            <TextView
                android:id="@+id/text_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_subtitle"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurface"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Engine and transport badges. Wrapping keeps both readable when
                 the user runs a large font scale instead of clipping one. -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_2dp"
                android:orientation="horizontal">

                <!-- Engine Badge -->
                <TextView
                    android:id="@+id/text_engine"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="@dimen/space_sm"
                    android:textSize="@dimen/text_size_overline"
                    android:textStyle="bold"
                    android:textColor="?attr/colorOnPrimary"
                    android:background="@drawable/badge_background"
                    android:paddingHorizontal="@dimen/space_sm"
                    android:paddingVertical="@dimen/space_2dp" />

                <!-- The wrapper takes the leftover width so the badge inside can
                     ellipsize instead of being clipped by the row. -->
                <FrameLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1">

                    <!-- Transport Mode Badge -->
                    <TextView
                        android:id="@+id/text_transport_mode"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textSize="@dimen/text_size_overline"
                        android:textStyle="bold"
                        android:textColor="?attr/colorOnSecondaryContainer"
                        android:background="@drawable/badge_background_secondary"
                        android:paddingHorizontal="@dimen/space_sm"
                        android:paddingVertical="@dimen/space_2dp"
                        android:maxLines="1"
                        android:ellipsize="end" />

                </FrameLayout>

            </LinearLayout>

            <!-- Linked Connection -->
            <TextView
                android:id="@+id/text_connection"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_xs"
                android:textSize="@dimen/text_size_small_body"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Last Connected -->
            <TextView
                android:id="@+id/text_last_connected"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_2dp"
                android:textSize="@dimen/text_size_caption"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:maxLines="1"
                android:ellipsize="end" />

        </LinearLayout>

        <!-- Overflow: host action sheet (same as long-press) -->
        <ImageButton
            android:id="@+id/button_more"
            android:layout_width="@dimen/min_touch_target"
            android:layout_height="@dimen/min_touch_target"
            android:layout_gravity="center_vertical"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/container_more_actions_desc"
            android:src="@drawable/ic_more_vert"
            app:tint="?attr/colorOnSurfaceVariant" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
