<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/space_sm"
    android:layout_marginEnd="@dimen/space_sm"
    android:layout_marginTop="@dimen/space_10dp"
    android:layout_marginBottom="@dimen/space_2dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorPrimary"
    app:cardBackgroundColor="?attr/colorSurfaceVariant">

    <!-- Single row: chevron + name | metrics (centered) | host count + action buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="@dimen/space_xs"
        android:paddingEnd="@dimen/space_xs"
        android:paddingTop="@dimen/space_6dp"
        android:paddingBottom="@dimen/space_6dp">

        <!-- Expand/collapse chevron -->
        <ImageButton
            android:id="@+id/btn_toggle"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dashboard_group_toggle_description"
            app:srcCompat="@drawable/ic_expand_more"
            app:tint="?attr/colorOnSurfaceVariant" />

        <!-- Group name -->
        <TextView
            android:id="@+id/tv_group_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Material3.TitleSmall"
            android:textColor="?attr/colorOnSurface"
            android:ellipsize="end"
            android:maxLines="1"
            android:paddingStart="@dimen/space_xs"
            android:paddingEnd="@dimen/space_xs" />

        <!-- Aggregated group metrics snapshot — centered in remaining space -->
        <TextView
            android:id="@+id/tv_group_metrics"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:fontFamily="monospace"
            android:visibility="gone" />

        <!-- Host count chip -->
        <TextView
            android:id="@+id/tv_host_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/space_6dp"
            android:paddingEnd="@dimen/space_6dp"
            android:paddingTop="@dimen/space_2dp"
            android:paddingBottom="@dimen/space_2dp"
            android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:background="@drawable/bg_host_count_chip"
            android:layout_marginEnd="@dimen/space_xs" />

        <!-- Add hosts to this group -->
        <ImageButton
            android:id="@+id/btn_add_hosts"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dashboard_group_add_hosts_description"
            app:srcCompat="@drawable/ic_add"
            app:tint="?attr/colorOnSurfaceVariant" />

        <!-- Bulk monitor config for all hosts in this group -->
        <ImageButton
            android:id="@+id/btn_bulk_monitor"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dashboard_group_bulk_monitor_description"
            app:srcCompat="@drawable/ic_flash"
            app:tint="?attr/colorOnSurfaceVariant" />

        <!-- Rename group -->
        <ImageButton
            android:id="@+id/btn_rename"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dashboard_group_rename_description"
            app:srcCompat="@drawable/ic_edit"
            app:tint="?attr/colorOnSurfaceVariant" />

        <!-- Delete group -->
        <ImageButton
            android:id="@+id/btn_delete"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dashboard_group_delete_description"
            app:srcCompat="@drawable/ic_close"
            app:tint="?attr/colorOnSurfaceVariant" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
