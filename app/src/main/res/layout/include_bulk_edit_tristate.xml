<?xml version="1.0" encoding="utf-8"?>
<!--
    Bulk-edit tri-state row (rewrite, 2026-05-02).

    Vertical layout so the label never has to fight the segmented control
    for horizontal space. "Don't change" is the default state and reads
    as words instead of an em-dash so the meaning is obvious. Off / On
    are the explicit choices.

    The fragment populates `tri_label` + `tri_icon` per row at bind time.
    `tri_group` ID is referenced by ConnectionsFragment for state lookup.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="@dimen/space_sm"
    android:paddingBottom="@dimen/space_sm">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="@dimen/space_6dp">

        <ImageView
            android:id="@+id/tri_icon"
            android:layout_width="@dimen/icon_size_action"
            android:layout_height="@dimen/icon_size_action"
            android:layout_marginEnd="@dimen/space_12dp"
            android:src="@drawable/ic_settings_general"
            app:tint="?attr/colorOnSurfaceVariant"
            android:importantForAccessibility="no" />

        <TextView
            android:id="@+id/tri_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text=""
            android:textColor="?attr/colorOnSurface"
            android:textAppearance="?attr/textAppearanceBodyLarge" />
    </LinearLayout>

    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/tri_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:singleSelection="true"
        app:selectionRequired="true">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/tri_unchanged"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_weight="1.4"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/dashboard_stat_value_width"
            android:insetTop="@dimen/space_none"
            android:insetBottom="@dimen/space_none"
            android:paddingHorizontal="@dimen/space_sm"
            android:textAllCaps="false"
            android:textAppearance="?attr/textAppearanceLabelMedium"
            android:text="@string/include_bulk_edit_tristate_unchanged" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/tri_off"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/dashboard_stat_value_width"
            android:insetTop="@dimen/space_none"
            android:insetBottom="@dimen/space_none"
            android:paddingHorizontal="@dimen/space_sm"
            android:textAllCaps="false"
            android:textAppearance="?attr/textAppearanceLabelMedium"
            android:text="@string/include_bulk_edit_tristate_off" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/tri_on"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/dashboard_stat_value_width"
            android:insetTop="@dimen/space_none"
            android:insetBottom="@dimen/space_none"
            android:paddingHorizontal="@dimen/space_sm"
            android:textAllCaps="false"
            android:textAppearance="?attr/textAppearanceLabelMedium"
            android:text="@string/include_bulk_edit_tristate_on" />

    </com.google.android.material.button.MaterialButtonToggleGroup>

</LinearLayout>
