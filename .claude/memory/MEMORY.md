# Project Memory Index

Durable, project-specific knowledge discovered during development —
decisions, gotchas, conventions unique to this codebase. Read the
referenced file on demand when its topic is relevant.

- `gitcommit_gate_override.md` — standing authorization to bypass the
  test/lint gate marker on `gitcommit` when `make check`/`make test`
  already passed, due to a recurring upstream marker bug in this
  project.
